# 🎹 Piano Platform

Приложение для обучения игре на фортепиано, вдохновленное Synthesia. Написано на C++ с использованием Qt 6.

## Версия 1.0 - Основной функционал

### Возможности:
- ✅ Загрузка MIDI файлов
- ✅ Воспроизведение с управлением темпо
- ✅ Прогресс-бар с отслеживанием времени
- ✅ Выбор инструментов
- ✅ Интерактивный интерфейс на Qt 6

### Требования

#### Обязательные компоненты:

**Windows:**
- Visual Studio 2019/2022 (или MinGW)
- CMake 3.20+
- Qt 6.x
- Git (опционально)

**Linux (Ubuntu/Debian):**
- GCC 10+ или Clang 10+
- CMake 3.20+
- Qt 6.x
- Build essentials

**macOS:**
- Xcode 13+
- CMake 3.20+
- Qt 6.x via Homebrew

## Установка и сборка

### Шаг 1: Подготовка

```bash
# Клонируем/скачиваем проект
git clone <repo-url>
cd PianoPlatform

# Или создаем вручную структуру проекта:
mkdir -p src include build
```

### Шаг 2: Скачивание RtMidi (опционально)

```bash
# В папке проекта
git clone https://github.com/thestk/rtmidi.git
# или скачиваем ZIP и распаковываем в папку rtmidi/
```

### Шаг 3: Сборка проекта

#### **Windows (Visual Studio)**

```bash
cd build
cmake .. -G "Visual Studio 16 2019"
# или для VS 2022:
cmake .. -G "Visual Studio 17 2022"

# Собираем
cmake --build . --config Release
```

#### **Windows (MinGW)**

```bash
cd build
cmake .. -G "MinGW Makefiles"
cmake --build . --config Release
```

#### **Linux/macOS**

```bash
cd build
cmake ..
make -j$(nproc)  # Linux
make -j$(sysctl -n hw.ncpu)  # macOS
```

### Шаг 4: Запуск

```bash
# Windows
.\Release\PianoPlatform.exe

# Linux/macOS
./PianoPlatform
```

## Структура проекта

```
PianoPlatform/
├── CMakeLists.txt              # Конфиг сборки
├── README.md                   # Этот файл
├── src/
│   ├── main.cpp               # Точка входа
│   ├── MainWindow.h           # Главное окно (заголовок)
│   ├── MainWindow.cpp         # Главное окно (реализация)
│   ├── MidiPlayer.h           # MIDI плеер (заголовок)
│   ├── MidiPlayer.cpp         # MIDI плеер (реализация)
│   ├── MidiParser.h           # Парсер MIDI (заголовок)
│   └── MidiParser.cpp         # Парсер MIDI (реализация)
├── include/                    # Доп. заголовки (для расширения)
├── rtmidi/                     # Библиотека RtMidi (опционально)
└── build/                      # Папка для сборки (создается автоматически)
```

## Архитектура

### Основные классы:

**MainWindow**
- Главное окно приложения
- Управление UI элементами (кнопки, слайдеры)
- Обработка пользовательских действий

**MidiPlayer**
- Управление воспроизведением
- Синхронизация времени
- Сигналы для обновления UI

**MidiParser**
- Парсинг MIDI файлов
- Извлечение нот и событий
- Вычисление длительности

## Следующие этапы разработки

### Версия 1.1:
- Визуализация клавиш фортепиано
- Анимация падающих нот
- Синтез звука или использование SF2 сэмплов

### Версия 1.2:
- Детектор нот (проверка игры пользователя)
- Система скоринга
- Режим обучения

### Версия 2.0:
- Полная визуализация как Synthesia
- Сохранение прогресса
- Рейтинговая система
- Поддержка множественных режимов игры

## Решение проблем

### Qt не найден при сборке

```bash
# На Linux, установите Qt6:
sudo apt install qt6-base-dev qt6-tools-dev

# На macOS:
brew install qt6
```

### CMake не найден

```bash
# Windows: скачиваем с https://cmake.org
# Linux: sudo apt install cmake
# macOS: brew install cmake
```

### Ошибки компиляции MIDI

Убедитесь, что RtMidi скачан в папку `rtmidi/` или отключите его в CMakeLists.txt

## Лицензия

MIT License - свободно используйте и модифицируйте!

## Контакты и поддержка

Для вопросов и предложений создавайте Issues в репозитории.

---

**Версия:** 1.0.0  
**Последнее обновление:** Декабрь 2025  
**Статус:** В активной разработке 🚀
